# ============================================================
# NextWeight — 환경변수 목록 (.env.example)
# 실제 값은 .env.local 에 설정하고 git에는 절대 커밋하지 마세요.
# ============================================================

# ── 공개 변수 (클라이언트에서 접근 가능) ─────────────────────
# Google Analytics 측정 ID (선택)
NEXT_PUBLIC_GA_ID=

# 공유 링크 생성 시 사용할 기본 URL (서버 사이드 fetch용)
# 예: https://nextweight.co.kr
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ── 인증 (Auth) ─────────────────────────────────────────────
# 세션 쿠키 서명용 비밀키 (최소 32자 이상의 무작위 문자열)
SESSION_SECRET=change-me-to-a-random-32-char-secret

# OTP HMAC 서명용 비밀키 (SESSION_SECRET 과 다른 값 사용 권장)
OTP_SECRET=change-me-to-another-random-32-char-secret

# 개발 환경 전용: 이 값이 설정되면 OTP 검증 시 해당 코드를 항상 허용
# 프로덕션에서는 반드시 제거하거나 비워두세요
MOCK_OTP=123456

# ── Airtable ─────────────────────────────────────────────────
# Airtable Personal Access Token
AIRTABLE_PAT=

# Airtable Base ID (예: appXXXXXXXXXXXXXX)
AIRTABLE_BASE_ID=

# ─ 환자 중심 플랫폼 테이블 ────────────────────────────────────
# 사용자 테이블 (기본값: "Users")
AIRTABLE_TABLE_USERS=Users

# 일일 기록 테이블 (기본값: "DailyLogs")
AIRTABLE_TABLE_DAILY_LOGS=DailyLogs

# 주간 기록 테이블 (기본값: "WeeklyLogs")
AIRTABLE_TABLE_WEEKLY_LOGS=WeeklyLogs

# 공유 토큰 테이블 (기본값: "ShareTokens")
AIRTABLE_TABLE_SHARE_TOKENS=ShareTokens

# ─ [Legacy] 병원 모니터링 테이블 (레거시 경로 유지 시에만 필요) ──
# 환자 테이블 (기본값: "Patients")
AIRTABLE_TABLE_PATIENTS=Patients

# 일일 설문 테이블 (기본값: "DailyResponses")
AIRTABLE_TABLE_DAILY=DailyResponses

# 주간 설문 테이블 (기본값: "WeeklyResponses")
AIRTABLE_TABLE_WEEKLY=WeeklyResponses

# ─ [Legacy] 병원 토큰 인증 (레거시 경로 유지 시에만 필요) ───────
# JSON 형식: { "clinic_id": "token" }
# 예: {"C001": "secret-token-here"}
CLINIC_TOKEN_MAP={}

# 인증 모드 (현재: "token")
AUTH_MODE=token

# ── [Legacy] Google Sheets ───────────────────────────────────
# 서비스 계정 이메일
GOOGLE_SHEETS_CLIENT_EMAIL=

# 서비스 계정 개인키 (줄바꿈은 \n 으로 대체)
GOOGLE_SHEETS_PRIVATE_KEY=

# Google Sheets ID
GOOGLE_SHEET_ID=
